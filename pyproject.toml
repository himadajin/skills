[project]
name = "himadajin-skills"
version = "0.0.0"
description = "Validation tooling for Himadajin's personal Agent Skills"
requires-python = ">=3.11"
dependencies = [
    "ruamel-yaml>=0.18.15,<0.19",
]

[tool.uv]
package = false
required-version = "==0.12.5"
